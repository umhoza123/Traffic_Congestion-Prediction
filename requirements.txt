import pickle
import numpy as np
import streamlit as st
with open(r'C:\Users\Rachel\Desktop\data set\ving.pkl', 'rb') as f:
    model=pickle.load(f)
st.title("traffic congestion")
st.write("fill the following information") 
#week=st.selectbox("Day of week",['Mon','Tue','Wed','Thur','Fri','Sat','Sun'])
#unt=st.number_input("CarCount")
#BikeCount=st.number_input("BikeCount")
#BusCount=st.number_input("BusCount")
#Total=st.number_input("Total")

#if week=='Mon':
    days=1
#if week=='Tue':
    days=2
#if week=='wed':
    days=3
#if week=='Thur':
    days=4
#if week=='fri':
    days=5
#if week=='sat':
    days=6
#if week=='sun':
    days=7
CarCount = int(st.number_input("Enter car count"))
BikeCount = int(st.number_input("Enter bike count"))
BusCount = int(st.number_input("Enter bus count"))
Total = CarCount + BikeCount + BusCount
days = st.selectbox("Select day", ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"])
day_mapping = {
    "Monday": 1, "Tuesday": 2, "Wednesday": 3,
    "Thursday": 4, "Friday": 5, "Saturday": 6, "Sunday": 7
}
days = day_mapping[days]

    
        

if st.button("predict"):
    data=np.array([[CarCount,BikeCount,BusCount,Total,days]])
    pred=model.predict(data)
    st.success(f"traffic congestion:{pred[0]}")